#include <stdio.h>
int main (void){
	 int inicial, final, i;

 
   printf( "Digite um numero inicial: ");
   scanf("%d",&inicial);
   printf( "Digite um numero final: ");
   scanf("%d",&final);
     for (int i = inicial; i <= final; i++);
      printf("%d",i);
}